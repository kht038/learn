# list_name = [data, a, b, c]

# data append list.append(data)
# 데이터 할당 - list[index] = data
# delete data = del list[index
# list[start:end + 1] - list slicing
# list[:] list all
# len(list) - list length
# list.sort() - list sorting
# list.sort(reverse=True) 역순 정렬