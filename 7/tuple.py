# 시퀀스 자료형
# 수정, 추가, 삭제가 불가능한 리스트
# 메모리 사용이 효율적
# 읽기만 가능하여 데이터 손실의 염려가 없음

# tuple = (0,1,data)

# 튜플을 리스트로 만들기
# a = tuple([5,6,7])
# x = list(range(10))
# a = tuple(x)

# 패킹과 언패킹
# numbers = 3,4,5 # 패킹
# a, b, c = numbers # 언패킹

# 패킹 : 여러 개의 데이터를 하나의 변수에 할당하는 것
# 언패킹 : 컬렉션의 각 데이터를 각각의 변수에 할당하는 것

# 튜플 함수
# 특정값의 인덱스 구하기
# a.index(20)
# 특정값의 개수
# a.count(30)
# 최대값 최소값
# max(a), min(a)
# 합계
# sum(a)